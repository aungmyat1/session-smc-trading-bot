{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agtrade.local/schemas/strategy-package-v2.schema.json",
  "title": "Canonical Strategy Package v2 Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "package_format",
    "package_id",
    "strategy_id",
    "strategy_version",
    "adapter_id",
    "adapter_version",
    "adapter_code_sha256",
    "runtime_api_version",
    "instruments",
    "timeframes",
    "parameter_sha256",
    "execution_policy_sha256",
    "symbols",
    "live_trading_enabled",
    "members"
  ],
  "properties": {
    "package_format": {"const": "strategy-package/v2"},
    "package_id": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "strategy_id": {"type": "string", "minLength": 1},
    "strategy_version": {"type": "string", "minLength": 1},
    "adapter_id": {"type": "string", "minLength": 1},
    "adapter_version": {"type": "string", "minLength": 1},
    "adapter_code_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "runtime_api_version": {"const": "system2-runtime/v1"},
    "instruments": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
    "timeframes": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
    "parameter_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "execution_policy_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "symbols": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
    "live_trading_enabled": {"const": false},
    "members": {
      "type": "object",
      "minProperties": 6,
      "additionalProperties": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
    }
  }
}
