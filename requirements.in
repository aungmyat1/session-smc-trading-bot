# Core runtime and configuration
python-dotenv>=1.0,<2
aiohttp>=3.9,<4
aiofiles>=23,<25
requests>=2.31,<3
pyyaml>=6,<7
pydantic>=2.8,<3
jsonschema>=4.20,<5

# Control-plane persistence
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9,<3
alembic>=1.13,<2

# Research data and analytics
polars>=0.20,<2
pyarrow>=14,<25
duckdb>=1.5,<2
pandas>=2.2,<3

# Compatibility HTTP interface
flask>=3.0,<4
flask-cors>=4,<7
fastapi>=0.115,<1
uvicorn>=0.30,<1

# Gemini AI — current unified SDK for Developer API and Vertex AI
google-genai>=2.10,<3

# Legacy Vantage/MetaAPI execution adapter — retained until ADR-0011 mt5linux
# cutover completes shadow verification (see execution/metaapi_client.py)
metaapi-cloud-sdk>=29,<30

# Vantage execution adapter bridge (ADR-0011) — client side only; requires a
# Wine-hosted MT5 terminal + `python -m mt5linux <wine-python.exe>` RPyC server
# running on the VPS (see docs/svos/ADR-0011-MT5LINUX-BROKER-BRIDGE.md)
mt5linux>=0.1.9,<1
